[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "expense-tracker"
version = "0.1.0"

[project.scripts]
et = "src.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]